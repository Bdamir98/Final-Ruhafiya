# Example environment variables (do NOT commit real secrets)
# Copy this file to `.env.local` and fill in real values for local dev.
# On Vercel, add the SAME KEYS in Project Settings → Environment Variables.

# Naming rules:
# - Keys that start with NEXT_PUBLIC_ are exposed to the browser (public).
# - Non NEXT_PUBLIC_* keys are server-only (keep secret).

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT_ID.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"  # server-only
# Optional: If you need a client anon instance somewhere, prefer using the anon key.
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-public-key"

# App
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Facebook Pixel & Conversion API
# Public: Pixel ID exposed to client for fbq init
NEXT_PUBLIC_FB_PIXEL_ID=""
# Testing: Use the Test Event Code from Events Manager while testing
NEXT_PUBLIC_FB_TEST_EVENT_CODE=""
# Server-only: Long-lived system user token for CAPI requests
FB_ACCESS_TOKEN=""

# Google Tag Manager
NEXT_PUBLIC_GTM_ID=""

# Google Analytics 4
NEXT_PUBLIC_GA_ID=""